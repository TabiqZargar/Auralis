interface PlayButtonProps {
  onClick: () => void;
  isPlaying?: boolean;
  size?: "sm" | "md" | "lg";
  variant?: "primary" | "secondary";
  disabled?: boolean;
}

export function PlayButton(_props: PlayButtonProps) {
  return null;
}
